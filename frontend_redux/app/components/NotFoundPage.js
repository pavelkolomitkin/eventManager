import React from 'react';

const NotFoundPage = (props) => {
    return (
        <div>
            <p className="alert alert-info">Page not found</p>
        </div>

    );
};

export default NotFoundPage;
