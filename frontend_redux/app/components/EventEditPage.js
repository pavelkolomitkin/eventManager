import React from 'react';
import PropTypes from 'prop-types';

const EventEditPage = (props) => {
    return (
        <div>
            <h1>This is event edit page</h1>
        </div>
    );
}

EventEditPage.propTypes = {};

export default EventEditPage;
