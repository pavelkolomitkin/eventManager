import React from 'react';
import PropTypes from 'prop-types';

const EventListPage = (props) => {
    return (
        <div>
            <h1>This is Event list page</h1>
        </div>
    );
}

EventListPage.propTypes = {};

export default EventListPage;
