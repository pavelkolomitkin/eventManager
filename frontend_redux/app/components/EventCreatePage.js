import React from 'react';
import PropTypes from 'prop-types';

const EventCreatePage = (props) => {
    return (
        <div>
            <h1>
                This is create new event page
            </h1>
        </div>
    );
}

EventCreatePage.propTypes = {};

export default EventCreatePage;
